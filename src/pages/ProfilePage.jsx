
import React from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../hooks/useAuth';import AppNav from '../components/AppNav';import '../styles/Profile.css';
export default function ProfilePage(){const {user,logout}=useAuth();const navigate=useNavigate();const name=user?.name||'AGRISENSE AI 🌿User';const initials=name.slice(0,1).toUpperCase();const date=user?.joinDate||user?.loginTime;return <div className="app-page profile-page"><AppNav/><main className="page-wrap"><div className="page-hero"><span className="eyebrow">Account</span><h1 className="page-title">Your profile</h1><p className="page-subtitle">Manage your AGRISENSE AI account and keep your workspace details handy.</p></div><section className="profile-layout"><div className="profile-main card"><div className="profile-cover"><div className="avatar-lg">{initials}</div><div><h2>{name}</h2><p>{user?.email}</p></div></div><div className="profile-body"><div className="profile-section"><span className="kicker">Account details</span><div className="profile-row"><span>Full name</span><b>{name}</b></div><div className="profile-row"><span>Email</span><b>{user?.email}</b></div><div className="profile-row"><span>Member since</span><b>{date?new Date(date).toLocaleDateString():'—'}</b></div></div><div className="profile-section"><span className="kicker">Preferences</span><button className="preference"><span>Notifications</span><b>On</b></button><button className="preference"><span>AI screening tips</span><b>On</b></button></div></div></div><aside className="profile-side"><div className="card profile-tip"><span className="tip-icon">✦</span><h3>Make your scans count</h3><p>Use natural light, keep the affected leaf in focus, and include enough of the plant for context.</p><button className="btn btn-primary" onClick={()=>navigate('/pest-detection')}>Start a scan →</button></div><button className="btn btn-danger logout-profile" onClick={()=>{logout();navigate('/login')}}>Sign out of AGRISENSE AI</button></aside></section></main></div>}
